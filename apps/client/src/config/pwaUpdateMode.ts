export type PWAUpdateMode = 'auto' | 'manual'

// Use 'auto' for forced app refreshes after major releases.
// Use 'manual' when users should see the update prompt and reload themselves.
export const PWA_UPDATE_MODE: PWAUpdateMode = 'auto'
